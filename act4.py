import math

class OptimizacionGrafica(object):
    def __init__(self, valoresx, valoresy):
        self.a
        self.b
        self.c
        self.d

        self.x = valoresx
        self.y = valoresy 
        self.ecuacion = (math.cos(self.a*self.x) + self.b * x + - c * x^2 + d)